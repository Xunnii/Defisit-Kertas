import React from 'react';
import { View, Text } from 'react-native';

export const ProfileScreen = () => {
    return (
        <View className="flex-1 items-center justify-center bg-primary">
            <Text className="text-gray-800 font-semibold">Profile</Text>
        </View>
    );
};


